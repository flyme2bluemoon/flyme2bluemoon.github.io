# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Deploy

on:
  push:
    branches: [ v3 ]
  pull_request:
    branches: [ v3 ]
  schedule:
    - cron: "0 5 */2 * *"

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v3
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm install
    - name: Deploy with gh-pages
      run: |
        git remote set-url origin https://git:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git
        npm run deploy -- -u "Matthew Shen (using Github Actions) <37197710+flyme2bluemoon@users.noreply.github.com>"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    # - name: Deploy on McGill CS Webserver
    #   run: |
    #     npm run clean
    #     npm run mimiBuild
    #     cd public/ && zip -r ../mimi.zip * && cd ..
    #     echo ${SSH_KEY} > id_rsa
    #     chmod 600 id_rsa
    #     scp -i id_rsa mimi.zip mshen@mimi.cs.mcgill.ca:~/public_html
    #     curl https://cs.mcgill.ca/~mshen/cgi-bin/rebuild.cgi?${API_KEY}
    #   env:
    #     SSH_KEY: ${{ secrets.SSH_KEY }}
    #     API_KEY: ${{ secrets.API_KEY }}
